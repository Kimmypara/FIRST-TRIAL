// Arrays are lists of objects / variables

let list = [];  //empty array
console.log(list);

list = ['Matt', 'Joe', 'Doris'];
console.log(list);

//create list with values
let colors = ['red', 'green', 'blue'];
console.log(colors);

//Accessing a value in an array based on its position
//P.S. positions always start at 0
console.log(colors[0]);

console.log(colors.length);










